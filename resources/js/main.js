/**
 * Inicialización de la aplicación.
 *
 * Escriba dentro de la función MyAppInit() la carga de archivos JS/CSS con
 * Modernizr. ( http://modernizr.com/docs/#load )
 *
 * Nota: Para este paso ya se han cargado todos los módulos necesarios:
 * Kernel, Geo, Data, etc (Siempre y cuando lo soporte el navegador)
 *
 * Para ver todos los módulos cargados use la consola de Google Chrome.
 * Puede usar la función Load($file) para cargar archivos directamente en RESOURCES
 *
**/

function MyAppInit()
{
	//Load('app.page.js');
	//Load('app.style.css');
}